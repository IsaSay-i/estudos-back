const Button = () => {
  return (
    <>
      {/* parâmetros = tipo(login, normal, addEntry)
                       cores(tr-blue, tr-orange, tr-red, green, red)  tr -> transparente 
          a função do botão, como eu faria? eu deixo esse componente só como uma estrutura visual e deixo a função do botão dentro da tag <Button> </Button> ooou tem algum outro jeito? */}
    </>
  )
}

export default Button;